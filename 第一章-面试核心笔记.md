# 第一章《基础概念入门》面试核心笔记

> 用途：基于第一章学习材料提炼，覆盖面试常考查知识方向与高频考点，便于快速复习与备考。

## 一、本章面试考查方向总览

面试官通常从以下四个维度考查第一章内容：

| 考查维度 | 典型问法 | 对应小节 |
|---|---|---|
| **概念辨析** | "Chatbot、Workflow、Agent 有什么区别？" | 1.1 / 1.5 |
| **原理认知** | "Token 是什么？上下文窗口越大越好吗？" | 1.2 |
| **工程权衡** | "怎么抑制幻觉？为什么不用长上下文代替检索？" | 1.4 |
| **边界判断** | "什么场景该用 Agent，什么场景不该用？" | 1.1 / 1.5 |

---

## 二、逐节核心笔记

### 1.1 什么是 AI Agent（最高频）

**核心定义**：AI Agent 是能**自主感知环境、做出决策、采取行动**以完成目标的 AI 系统；区别于被动的"输入→输出"式 AI，本质特征是**自主性**。

**最小闭环公式**：`Agent = LLM（大脑）+ 工具（手）+ 记忆（脑容量）+ 循环决策（自主性）`，四要素缺一不可。

**五大能力**：感知 / 推理 / 行动 / 记忆 / 自主决策。

**四个关键特征**（把 Chatbot 升级成 Agent 多给的三样 + 自我反思）：
1. 自主规划（拆子步骤）2. 工具使用（动手而非动嘴）3. 记忆（跨轮上下文）4. 自我反思（判断并修正）

**高频对比 1 — Agent vs Chatbot vs Workflow**：核心区别是**路径的动态性**。Workflow 路径由 DAG 预定义、固定；Agent 根据中间结果走完全不同的路径。

**高频对比 2 — 自主性谱系**（面试常让排序）：
`规则脚本(零自主) → RPA(低) → 工作流(中低) → Copilot(人主导) → 单 Agent(高) → 多 Agent(极高)`
> ⚠️ 不是越自主越好，要匹配任务的**不确定性与风险**；订票、转账等错了代价高的任务，反而应收回自主、让人确认。

**易错点 / 高频考点**：
- 🔴 **没有终止条件** → 最小 Agent 必须有 `max_steps` 循环护栏（如 `range(5)`）。
- 🔴 **为用 Agent 而用 Agent** → 步骤固定、风险高需人确认的场景不该上 Agent。

---

### 1.2 LLM 工作原理基础

**Token（必考题）**：LLM 处理文本的**最小单位**，不是字符也不是单词。
- 英文 ≈ 1 token / 0.75 词；中文 1 汉字 ≈ 1~2 token；代码最费 token。
- **误区**："中文更省 token"是错的。价格、限速、上下文上限全部按 token 计。
- 多轮对话中 `system + 历史`持续累积，需记忆裁剪/摘要（否则后半段"失忆"）。

**Context Window（上下文窗口）**：一次能处理的最大 token 数（含输入+输出）。
- ⚠️ **上下文窗口 ≠ 推理窗口**：长上下文下模型对中段注意力衰减（lost in the middle），且每轮重算使延迟/成本线性上升。
- 🔴 **易错点：用长上下文代替检索** → 应分块/检索/摘要挤有效信息。

**采样参数**：

| 参数 | 作用 | 推荐值 |
|---|---|---|
| Temperature | 0=确定性，越高越随机 | 代码0 / 客服0.3 / 创意0.9 / 严谨研究0+CoT |
| Top-P | 核采样，只看累计概率前 p 的候选 | 0.9~1.0，与 Temp 二选一 |
| max_tokens | 限制输出长度 | 500~4096 |

> ⚠️ Temperature / Top-p 控制的是**确定性不是质量**；误区"调高温度更聪明"——只是更随机。

**System Prompt**：对话第一条消息，定义角色与行为规则。🔴 易错点：**太长或被注入**。

---

### 1.3 Prompt Engineering 入门

**三大核心原则**：清晰、具体、结构化。

**Zero-shot vs Few-shot**：
- Few-shot 本质 = **"用例子定义任务边界"**，模型从示例归纳而非文字描述。
- 示例要覆盖边界情况（中立、矛盾情绪），否则边界翻车。

**CoT（思维链）**：让模型一步步思考，提升数学/逻辑推理；代价是变长、变慢、变贵。

**Role Prompting**：设定身份激活对应语料分布，**不提升事实准确性**（只调口吻）。

**六大反模式（高频）**：模糊指令 / 矛盾约束 / 过度约束 / 缺乏示例 / 角色模糊 / 开放过度。

**工程要点**：
- 用 `<tag>` / ```` ``` ```` 结构化分隔符让模型分清区块边界。
- 💡 **Prompt 是每次调用都付费的"代码"** → 要像代码优化（删冗余、抽公共、高频简单任务换小模型）。
- **位置偏差**：开头(system)与结尾(当前任务)最敏感，关键约束放首尾。

---

### 1.4 模型能力与局限

**幻觉 Hallucination（必考题）**：生成看似合理但错误/不存在/误导的内容。四类：
`事实幻觉 / 代码幻觉 / 引用幻觉 / 推理幻觉`。

> 💡 **核心认知**：幻觉不是 bug，是概率模型的**本性**——训练目标是"预测下一个最合理 token"而非"保证事实为真"。抑制只能靠**架构**（RAG 给真资料、工具查真数据、结构化校验、拒答兜底），不能靠"让模型更诚实"。

**知识截止日期** → RAG 的核心动机。

**能力评估 Benchmark**：MMLU（综合知识）、GSM8K（数学）、HumanEval（代码）、GPQA（研究生级推理）、MATH（竞赛数学）。

**能力矩阵（架构设计关键）**：

| 能力 | 正确做法 |
|---|---|
| 事实问答 | RAG 喂资料 |
| 精确计算 | 调 Python/计算器 |
| 实时信息 | 调搜索/API |
| DB 读写 | 调工具+鉴权 |
| 格式约束 | 结构化输出+校验 |

> ⚠️ **选型 = 能力 × 成本 × 延迟**三角权衡；小模型前置过滤 + 大模型深度处理最省钱。
> 🔴 **易错点：把模型的"流畅"当"正确"** → 用拒答阈值兜底。

---

### 1.5 从 LLM 到 Agent 的演进

**Agent 三要素**：LLM（大脑）/ 工具（手，Function Calling·MCP·API）/ 循环决策（自主性），缺一不可。

**vs 传统软件**：传统软件**确定性**（同输入同输出、断言式测试、异常捕获）；Agent **概率性**（可能不同输出、评估式测试、重试+降级+Guardrail、Trace+Prompt 调试）。

**演进是分层不是替代**：`LLM → +Prompt → +工具 → +规划 → +记忆 → +多Agent`；多数业务先用"单 Agent + 几个工具"。

**RPA ≠ Agent**：RPA 写死步骤点界面，流程一变就崩；Agent 在"目标"层面工作能自调。常组合：**Agent 决策 + RPA 执行**。

**Copilot vs Agent（高频对比）**：流程控制权在**人** vs **模型**；失败影响小 vs 大；适合创作/分析 vs 执行/自动化。

> 💡 **2023 后是能力临界点而非技术突变**：ReAct 2022 已提出，模型够强才实用 → 判断值不值得上 Agent，看**当下模型真实能力**而非论文新旧。
> 🔴 **易错点**：为用 Agent 而用 Agent；把所有逻辑塞进 prompt。

---

### 1.6 学习路线图（了解即可，较少深考）

- 6 章路线：基础(8h) → 核心原理(20h) → 框架实战(18h) → 多Agent(15h) → 行业应用(25h) → 前沿(10h)。
- **里程碑要"可验证"**（能跑通代码）而非"看过"。
- **T 型能力模型**：广度打底 + 深度突破；**项目驱动 > 课程驱动**。

---

## 三、面试高频考点 30 秒速查

| 高频问题 | 一句话答案 |
|---|---|
| Agent 是什么？ | 自主感知-决策-行动的闭环系统 = LLM+工具+记忆+循环决策 |
| Agent 与 Chatbot 区别？ | 路径动态性：Agent 按结果自调路径，Chatbot 固定输入输出 |
| Token 是什么？ | LLM 最小处理单位，非字符非单词；决定成本/限速/上限 |
| 上下文窗口越大越好？ | 否；长上下文注意力衰减、成本延迟上升，应检索+摘要 |
| 怎么抑制幻觉？ | 靠架构（RAG/工具/校验/拒答），非靠提示；幻觉是概率本性 |
| 何时不该用 Agent？ | 步骤固定、风险高需人确认的场景 |
| RPA 和 Agent 区别？ | RPA 写死步骤，Agent 目标层自调；可组合 |
| Copilot 与 Agent 区别？ | 流程控制权在人 vs 模型 |
| Temperature 调高更聪明？ | 否，只是更随机；控的是确定性非质量 |
| 为什么 Few-shot 有效？ | 模型从示例归纳模式，而非从文字描述 |

---

## 四、典型面试题预测与答题要点

**Q1：请画一个最小 Agent 的工作流并说明每步。**
A：① 感知（读用户输入/工具返回/记忆）→ ② 决策（LLM 判断：回答/调工具/结束）→ ③ 行动（调工具或回复）→ ④ 结果写回感知，进入下一轮。关键是**闭环 + 终止条件（max_steps 护栏）**。

**Q2：为什么不用超长上下文窗口替代 RAG？**
A：上下文是有限 RAM，"能放下"≠"能用好"；长上下文中段注意力衰减、每轮重算成本延迟线性上升；超窗口只能靠架构层（分块/检索/压缩）解决，非调参。

**Q3：如何设计一个可靠的 Agent 系统？**
A：用工具/检索/校验补模型短板（幻觉、实时、计算），用人工护栏兜错误（高危动作人审批），用拒答阈值防编造，用 Trace+评估式测试保障可观测。
