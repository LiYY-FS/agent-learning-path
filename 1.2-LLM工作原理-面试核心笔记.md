# 第 1.2 章《LLM 工作原理基础》面试核心笔记

> 用途：基于重写后的 1.2 章节提炼，覆盖面试常考查的 LLM 工作原理知识与高频考点，便于快速复习与备考。

## 一、本章面试考查方向

| 考查维度 | 典型问法 | 对应知识点 |
|---|---|---|
| 本质认知 | "LLM 到底是什么？为什么说是概率模型？" | LLM 本质 / 四阶段 |
| 输入处理 | "文本怎么变成模型能算的东西？Embedding 和位置编码是什么？" | 分词 / 嵌入 / 位置编码 |
| 模型结构 | "Transformer 结构？自注意力怎么算？为什么比 RNN 好？" | Transformer / 自注意力 |
| 训练推理 | "模型怎么训练出来的？训练和推理有什么区别？" | 预训练 / 下一词预测 / 自回归 |
| 工程权衡 | "为什么不用超长上下文代替检索？Temperature 调高能更聪明吗？" | 上下文窗口 / 采样参数 / 终止条件 |

---

## 二、核心笔记

### 2.1 LLM 的本质（必考）

- **一句话定义**：LLM 是一个"**基于上下文预测下一个 token 的概率模型**"。它不"理解"文字含义，而是在海量文本上学会了"看到前文，猜下一个最可能出现的词"的统计规律。
- **四阶段工作流**（理解 LLM 的总框架）：
  1. **输入处理**：文字 → 分词 → 嵌入向量 + 位置编码
  2. **模型结构**：多层 Transformer（核心是自注意力）
  3. **训练**：海量文本上做"下一词预测"，反向传播更新参数
  4. **推理**：自回归地逐个生成 token，直到结束

### 2.2 输入处理：文字 → Token → 向量

- **分词（Tokenization）**：文本切成 token（子词），是模型读写的最小单位。
- **嵌入（Embedding）**：每个 token 映射为高维向量；语义相近的词，向量距离也相近——模型"懂"语义的起点。
- **位置编码（Positional Encoding）**：Transformer 本身不知道词序，需额外加入位置信息（"猫追狗"≠"狗追猫"）。
- **Token 估算经验法则**：英文 ≈ 1 词 1.3 token（4 字符 1 token）；中文 ≈ 1 汉字 1~2 token。价格、限速、上下文上限全部按 token 计。
- ⚠️ **易错点**：① 按字符估算成本会严重偏低；② 假设"同一段文本各模型 token 数相同"（不同分词器差异可达 30%+）；③ 忽略输出 token 成本；④ 截断按 token 而非字符。

### 2.3 模型结构：Transformer 与自注意力（高频）

- 现代 LLM 几乎都基于 **Transformer（Decoder 部分）**，核心是**自注意力（Self-Attention）**。
- **自注意力**：序列中每个 token 与所有 token 计算"相关性权重"，再按权重聚合信息 → 直接捕捉任意两词的长程依赖，"它"指代谁由注意力权重决定。
- **Transformer 块** = 自注意力子层 + 前馈网络(FFN)子层，各包残差连接 + 层归一化；模型由几十~上百个块堆叠。
- **为什么优于 RNN**：① 可并行（所有 token 同时算注意力）② 长程依赖（任意两词直接相连）③ 全局感受野。
- 💡 规模决定能力：参数量 / 层数 / 隐藏维度 / 注意力头数越大，容量越强（也更贵更慢）。

### 2.4 训练：模型是怎么"学会"的（必考）

- **预训练（自监督）**：给模型看前文若干 token，让它预测"下一个 token"（next-token prediction）。"下一个词"藏在文本里，无需人工标注。
- **损失与优化**：交叉熵损失衡量预测分布与真实词的差距 → 反向传播 + 梯度下降调整上百亿参数，让正确词概率越来越高。
- **关键特性**：① 学到的是统计规律，非真正因果推理；② 知识有**截止日期**（训练后的事不知道）→ 这正是 **RAG 的动机**；③ 训练资源极高（GPU 集群、数周）。

**训练 vs 推理 对比（高频）**：

| 维度 | 训练 Training | 推理 Inference |
|---|---|---|
| 目标 | 从海量文本学习语言与知识 | 用已学模型完成具体任务 |
| 监督信号 | 下一个词（自监督，无需标注） | 用户 Prompt（任务指令） |
| 是否更新参数 | 是（反向传播+梯度下降） | 否（参数冻结） |
| 并行性 | 可大规模并行 | 自回归，逐 token 串行 |
| 资源消耗 | 极高（集群、数周） | 较低，随输出长度线性增长 |

### 2.5 推理：模型是怎么"生成"的（高频）

- **自回归生成（auto-regressive）**：输入上文 → 输出下一个 token 概率分布 → 按解码策略选一个 → 拼回输入 → 再预测下一个，循环直到"结束"标记。
- **解码策略**：
  - **贪心解码（greedy）**：永远选概率最高的词 → 确定但易重复啰嗦。
  - **采样（sampling）**：按 Temperature / Top-p 从分布随机取词 → 更自然多样。
- ⚠️ **易错点：没有终止条件** → 自回归必须设 max_steps 或明确结束符，否则无限生成、烧光 token。

### 2.6 采样参数：Temperature 与 Top-P

- **Temperature**：0=确定性，越高越随机（0.0–2.0）。**控制"确定性"不是"质量"**。
- **Top-P（核采样）**：只从累计概率前 p 的候选词采样（0.0–1.0）。与 Temperature **二选一调**，不要同时拉满。
- ⚠️ 误区"调高温度更聪明"——不会，只是更随机。
- 推荐：代码生成 0 / 客服 0.3 / 创意 0.9 / 严谨研究 0（配合 CoT）。

### 2.7 上下文窗口与 System Prompt

- **Context Window**：一次能处理的最大 token 数（含输入+输出）。
  - ⚠️ **上下文窗口 ≠ 推理窗口**：长上下文中段注意力衰减（lost in the middle），每轮重算成本延迟线性上升 → 不要无脑堆历史，用 RAG/摘要/分窗口。
- **System Prompt**：对话第一条消息，定义角色与行为规则。🔴 易错点：太长或被注入（关键约束不止靠 system，工具/输出层再加校验）。

---

## 三、高频考点 30 秒速查

| 高频问题 | 一句话答案 |
|---|---|
| LLM 是什么？ | 基于上下文预测下一个 token 的概率模型，学的是统计规律而非真正理解 |
| 为什么需要 Embedding 和位置编码？ | 分词只是序号，嵌入把 token 变语义向量，位置编码补词序 |
| 自注意力怎么算？ | 每个 token 与全序列算相关性权重，按权重聚合上下文信息 |
| 为什么 Transformer 优于 RNN？ | 可并行、长程依赖、全局感受野 |
| 模型怎么训练？ | 预训练做下一词预测，交叉熵损失 + 反向传播更新参数 |
| 训练和推理的区别？ | 训练更新参数、可并行、自监督；推理冻结参数、自回归串行 |
| 推理为什么必须设终止条件？ | 自回归是循环，无上限会无限生成、烧光 token |
| Temperature 调高能更聪明？ | 否，只更随机；控的是确定性非质量 |
| 为什么不用长上下文代替检索？ | 长上下文注意力衰减、成本延迟上升，应 RAG 取相关片段 |
| 知识截止与 RAG 什么关系？ | 模型知识停在训练日且会编，RAG 注入实时/私有资料抑制幻觉 |

---

## 四、典型面试题预测与答题要点

**Q1：请说明一个 LLM 从输入文本到输出回答，内部经历了哪些步骤。**
A：① 输入处理——分词把文本切成 token，嵌入映射为高维向量，位置编码加入词序；② 模型计算——多层 Transformer 通过自注意力让每个 token 聚合全局上下文信息，输出下一个 token 的概率分布；③ 推理生成——按解码策略（贪心/采样）选一个 token，拼回输入循环预测，直到结束符。

**Q2：自注意力机制解决了 RNN 的哪些问题？**
A：RNN 逐词串行、难并行、长程依赖随距离衰减。自注意力让所有 token 同时计算、任意两词直接相连（全局感受野、无距离衰减），因此可大规模并行训练且擅长捕捉长距离依赖。

**Q3：为什么说"调高 Temperature 能让模型更聪明"是错的？**
A：Temperature 只控制输出的随机性/确定性，不改变模型的真实能力上限。调高只是让采样更发散、更随机，不会提升事实准确性；严谨任务反而应设 0 配合 CoT 以减少随机错误。

**Q4：训练阶段和推理阶段的核心区别是什么？**
A：训练用海量文本做自监督的"下一词预测"，通过反向传播更新参数（可并行、资源极高）；推理冻结参数，用训练好的模型对用户 Prompt 做自回归生成（逐 token 串行、资源随输出长度增长）。
